import React, {memo} from 'react';
import Layout from "@theme/Layout";

const Index = memo(() => {
    return (
        <>
            <Layout>
                myProject
            </Layout>

        </>
    );
});

export default Index;
